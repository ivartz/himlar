---
# network::interfaces_hash removed: legacy ifcfg keys (restart_all_nic) are not valid for
# raykrist/puppet-network nm::connection. Interface config is generated by
# profile::network::interface from named_interfaces + netcfg_* hiera.
{}
